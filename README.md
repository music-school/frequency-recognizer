# FrequencyRecognizerNode API

```js
const instance = FrequencyRecognizerNode.getInstance(
  audioContext,
  recognizer,
  recognizerListener
);

instance.init(); // start recognize frequency from media audio stream
instance.destroy(); // stop the recognizer
```
